
import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class CategoryListPage extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Pagina de categorias"),),
    );
  }
}
